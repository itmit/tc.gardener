<?xml version="1.0" encoding="UTF-8"?>

<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          xmlns:forms="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
          x:Class="gardener.Views.BlocksViews.NewsViewCell">
    <ViewCell.View>
        <Grid Margin="0,15,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" Grid.Column="0" Margin="10,0,10,0">
                <StackLayout VerticalOptions="CenterAndExpand" BackgroundColor="#f68b1f" Padding="1">
                    <forms:CachedImage Aspect="AspectFill" VerticalOptions="Center"
                                       ErrorPlaceholder="no_image"
                                       LoadingPlaceholder="no_image"
                                       Source="{Binding ImageUrl}" />
                </StackLayout>
            </StackLayout>
            <Label Grid.Row="0" Grid.Column="1" Text="{Binding Title}" Margin="10, 0" FontSize="Medium" TextColor="#9e4819"
                       VerticalTextAlignment="Center" HorizontalTextAlignment="Start" />
        </Grid>
    </ViewCell.View>
</ViewCell>