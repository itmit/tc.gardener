<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="gardener.Views.BlocksViews.CurrencyViewCell">
    <ViewCell.View>
        <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                <Image Source="{Binding Image}"/>
                <Label Text="{Binding CharCode}" FontSize="Large" Margin="0, 20" HorizontalOptions="Center"/>
            </StackLayout>
            <Label Grid.Column="1" Text="{Binding Value}" FontSize="Large" Margin="0, 20" HorizontalOptions="Center"/>
        </Grid>
    </ViewCell.View>
</ViewCell>