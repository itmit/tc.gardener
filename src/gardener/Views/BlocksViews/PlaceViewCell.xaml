<?xml version="1.0" encoding="UTF-8"?>

<gardener:ExtendedViewCell xmlns="http://xamarin.com/schemas/2014/forms"
                           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                           xmlns:gardener="clr-namespace:gardener"
                           x:Class="gardener.Views.BlocksViews.PlaceViewCell">
    <ViewCell.View>
        <StackLayout Margin="10, 10" Orientation="Horizontal">

            <Label Text="{Binding RowTitle}" FontSize="Large" TextColor="#333333" Margin="10, 0" />
            <Label Text="{Binding Row}" FontSize="Large" TextColor="#333333" />

            <Label Text="{Binding PlaceTitle}" FontSize="Large" TextColor="#333333" Margin="10, 0" />
            <Label Text="{Binding PlaceNumber}" FontSize="Large" TextColor="#333333" />

        </StackLayout>
    </ViewCell.View>
</gardener:ExtendedViewCell>